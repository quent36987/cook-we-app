import { UNKNOWN_ERROR_CODE } from '@angular/compiler-cli';

export enum ESeason {
  SPRING = 'printemps',
  SUMMER = 'été',
  AUTUMN = 'automne',
  WINTER = 'hiver',
  ALL = 'toutes les saisons',

  UNKNOWN = 'inconnue',
}
